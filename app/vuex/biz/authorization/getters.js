const userId = state => state.userId;
const sessionId = state => state.sessionId;
const phone = state => state.phone;
const loginInfo = state => state.loginInfo;
const phoneForResetPassword = state => state.phoneForResetPassword;

export default {
    userId,
    sessionId,
    phone,
    loginInfo,
    phoneForResetPassword,
};
